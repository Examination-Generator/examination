# Environment Variables Example
# Copy this file to .env.local and configure as needed

# API Base URL
REACT_APP_API_URL=http://localhost:8000/api

# Session Debug Info (Development Only)
# Set to 'true' to show session debug panel in bottom-right corner
# This will ONLY work in development mode (NODE_ENV=development)
# In production, this setting is completely ignored
REACT_APP_SHOW_SESSION_DEBUG=false

# How to enable session debug info:
# 1. Copy this file: cp .env.local.example .env.local
# 2. Edit .env.local and set: REACT_APP_SHOW_SESSION_DEBUG=true
# 3. Restart the development server: npm start
# 4. You'll see a small panel in the bottom-right showing:
#    - Session status (Active/Expired)
#    - Time remaining (in minutes)
#    - Last activity time
